spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:33008/ACE_db
    username: my_user
    password: my_password
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres
